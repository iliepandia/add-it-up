{
  "name": "add-it-up",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/bump-version.mjs && vite build && node scripts/postbuild.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.3.0",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
